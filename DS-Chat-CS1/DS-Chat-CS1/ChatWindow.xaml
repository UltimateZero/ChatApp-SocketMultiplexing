<Controls:MetroWindow x:Class="DS_Chat_CS1.ChatWindow"
       xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:DS_Chat_CS1"
        mc:Ignorable="d"   
        BorderThickness="1" 
        GlowBrush="{DynamicResource AccentColorBrush}"

        WindowStartupLocation="CenterScreen"
        Title="Chat Window" Height="400" Width="500" Background="{DynamicResource AccentColorBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid HorizontalAlignment="Right">
            <Button x:Name="btnSendImage" Content="Send Image" Click="btnSendImage_Click"/>
        </Grid>
        
        <!-- Chat messages -->
        <ScrollViewer Grid.Row="1" x:Name="ConversationScrollViewer"
                          HorizontalScrollBarVisibility="Hidden"
                          VerticalScrollBarVisibility="Hidden" ScrollChanged="ScrollViewer_ScrollChanged">
            <StackPanel Orientation="Vertical"
                      x:Name="ConversationContentContainer"
                      VerticalAlignment="Top">
                <local:ConversationView x:Name="conversationView"/>
            </StackPanel>
        </ScrollViewer>
        <!-- Chatbox -->
        <Grid Grid.Row="2" Margin="2,0,2,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="txtSend" Grid.Column="0" Controls:TextBoxHelper.Watermark="Enter your message..." AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap" MaxHeight="200" VerticalScrollBarVisibility="Auto" PreviewKeyDown="txtSend_PreviewKeyDown"/>
            <Button Content="Send" Grid.Column="1"/>
        </Grid>
    </Grid>
</Controls:MetroWindow>
